extends ../layout

block content
    h1= title
    nav
        ul
            li
                a(href='/admin/news/add') Add

    table
        tr
            th id
            th title
            th cratedAt
            th action
        each item, index in data
            tr
                td=index
                td=item.title
                td=item.createdAt
                td
                    a(href=`/admin/news/delete/${item._id}`) Delete
